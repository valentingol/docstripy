"""A test module.

Contains some functions for testing the parser.
"""


def my_func(alpha: float = 0.25) -> float:
    """Perform a computation.

    Args:
        alpha (float, optional): A parameter.
            Defaults to 0.25.
    """
    return alpha**2
